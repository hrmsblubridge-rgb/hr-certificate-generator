# Production runtime dependencies — only what is actually imported.
# Audited 2026-01-20 against backend/*.py:
#   server.py, auth.py, offer_letter.py, acknowledgement.py
# Anything not imported has been removed to keep the Render build fast and
# avoid wheel-availability problems (e.g. the internal-only
# emergentintegrations package; jq/typer/boto3/pandas/numpy left over from
# the project template).

# --- Web framework ---
fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette>=0.36.0,<0.37          # pinned by fastapi==0.110.1

# --- Data / validation ---
pydantic==2.6.4
pydantic-core==2.16.3            # explicit so prebuilt wheel is picked

# --- MongoDB (Atlas) ---
motor==3.3.1
pymongo==4.5.0
dnspython==2.6.1                 # required for `mongodb+srv://` URIs

# --- Auth ---
bcrypt==4.1.3
PyJWT==2.10.1

# --- PDF generation (Roboto/Arial baking + redaction pipeline) ---
pymupdf==1.27.2.3

# --- Env loading ---
python-dotenv==1.0.1
