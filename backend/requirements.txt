# Production runtime dependencies — only what is actually imported.
# Audited 2026-02 against backend/*.py:
#   server.py, auth.py, offer_letter.py, acknowledgement.py
# Targets Python 3.12.x on Render (see runtime.txt).
# No emergentintegrations (removed — caused Render wheel resolution failure).

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
# starlette is intentionally NOT pinned; fastapi resolves the compatible range.

# --- Data / validation ---
pydantic==2.10.4

# --- MongoDB (Atlas) ---
motor==3.6.0
pymongo==4.9.2
dnspython==2.7.0

# --- Auth ---
bcrypt==4.2.1
PyJWT==2.10.1

# --- PDF generation (Roboto/Arial baking + redaction pipeline) ---
pymupdf==1.27.2.3

# --- Env loading ---
python-dotenv==1.0.1
