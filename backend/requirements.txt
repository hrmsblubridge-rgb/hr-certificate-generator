# Production runtime dependencies — Render / Python 3.12.7
#
# Every version below ships a prebuilt wheel for cp312 / abi3 / py3 so
# `pip install -r requirements.txt` performs ZERO source compilation
# (audited 2026-02-20 against PyPI).
#
# DO NOT add `emergentintegrations` here — it is an internal-only package
# not published to PyPI and was the original cause of the Render build
# failure.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
# starlette is resolved transitively by fastapi==0.115.6 (>=0.40.0,<0.42.0)

# --- Data / validation ---
pydantic==2.10.4
pydantic-core==2.27.2          # pinned: ships cp312 PyO3 wheel; avoids
                               # compiling Rust from source on Render.

# --- MongoDB (Atlas) ---
motor==3.6.0
pymongo==4.9.2
dnspython==2.7.0               # required for `mongodb+srv://` URIs

# --- Auth ---
bcrypt==4.2.1                  # abi3 wheel — no compilation
PyJWT==2.10.1

# --- PDF generation (Roboto / Arial baking + redaction pipeline) ---
pymupdf==1.27.2.3              # abi3 wheel — no compilation

# --- Env loading ---
python-dotenv==1.0.1
