# PRD — Internship Certificate Editable Template

## Original Problem Statement
Convert the uploaded BluBridge Internship Certificate PDF into a reusable
template by turning four specific values — Name, Designation, Commenced on
Date, Concluded on Date — into manually editable fields for HR use, while
preserving the original document EXACTLY (font, size, weight, alignment,
spacing, margins, header, footer, logo, signature, layout).

## Architecture
- **Source PDF**: `backend/static/Aravind_Krishna_Original.pdf` (untouched copy)
- **Editable template**: `backend/static/Internship_Certificate_Template.pdf`
  generated by `work/make_editable.py` using PyMuPDF:
    1. `page.search_for()` locates the four target value strings
    2. `add_redact_annot(fill=white)` + `apply_redactions()` removes only
       those text glyphs (everything else — including the watermark logo,
       header, footer, signature, surrounding text — is untouched)
    3. AcroForm text widgets are inserted at the exact same rectangles
    4. AcroForm `/DR/Font/RobotoB` is wired to the document's already-embedded
       Roboto-Bold font (xref 9), and every widget `/DA` is patched to
       `0.137 0.137 0.231 rg /RobotoB 10 Tf`; `NeedAppearances=true` so any
       text the HR user types renders in Roboto-Bold 10pt matching the
       original value styling exactly
- **Backend (FastAPI)** — three endpoints under `/api/template/*`:
    - `GET /api/template/download` → editable PDF (attachment)
    - `GET /api/template/preview`  → editable PDF (inline, for iframe)
    - `GET /api/template/original` → original PDF
- **Frontend (React + Tailwind)** — single-page UI:
    - Header with primary "Download editable PDF" CTA
    - In-page iframe preview of the editable template
    - Toggle to compare with the original certificate
    - Sidebar listing the four editable fields and their original values

## Core Requirements (all met)
- [x] Original document preserved exactly (no font / size / weight / alignment / spacing / margin / header / footer / logo / signature / layout changes)
- [x] Only the four specified values converted into editable form fields
- [x] Fields appear in the same positions as the original values
- [x] Surrounding text unchanged
- [x] Blank by default (HR enters the values)
- [x] No additional fields, no wording changes, no design changes
- [x] Output is a downloadable PDF preserving original appearance

## Implemented
- **v15 (2026-02-25)** Internship Certificate now supports gender-aware pronouns. Added a required Gender dropdown (Male / Female) on the certificate form (`data-testid="cert-input-gender"`) between Designation and the date grid; native `<select>` styled to match existing inputs (zero new component dependencies). Backend `CertificateRequest` enforces `Literal["male","female"]` (422 on missing/invalid). For Male, the static body lines 3-4 of the source PDF (`"During his internship, he demonstrated..."` / `"We wish him all the best in him future endeavors."` — the existing "him future" typo is intentionally preserved per the user's "keep all existing content same except pronoun replacement" rule) are **NOT touched** — output is layout-identical to the pre-feature build. For Female, those two lines are redacted (single batched `apply_redactions` call together with the top paragraph) and re-rendered via `insert_htmlbox` with feminine pronouns at the exact original baselines (Δ=0.00pt vs source PDF, verified by `get_text("dict")`). Frontend live preview updates instantly on dropdown change via a `PRONOUNS` map. Gender persisted in `history.summary.gender`. Mobile-responsive (375px viewport verified, no horizontal scroll). Backend pytest 6/6 + frontend Playwright 8/8 passing (`/app/test_reports/iteration_2.json`).
- **v14 (2026-02-20)** Hardened admin credential storage. Removed the env-overwrite-on-boot behaviour: `seed_admin()` (which silently rewrote the DB hash from `HR_PASSWORD` on every restart) is replaced by `bootstrap_admin_if_missing()` which inserts a single admin **only when the row is missing** AND env credentials are present — never overwrites an existing hash. Added `POST /api/auth/change-password` (gated by `require_auth` + CSRF + brute-force lockout via the `:chpw` identifier suffix, min 8-char policy, must differ from current). The endpoint clears session cookies on success so the user re-logs in with the new password. New CLI bootstrap `backend/create_admin.py` for interactive admin creation / password reset (operator runs on Render shell when no env credentials are provided or to recover from a lost password — plain text never touches disk, logs, or shell history). Frontend: new "Change password" button in app header opens `ChangePasswordModal` (current / new / confirm with show-password toggles, inline validation, signs the user out on success). The MongoDB `users` collection is now the sole source of truth for credentials; `HR_USERNAME`/`HR_PASSWORD` env vars are first-deploy-bootstrap only.
- **v13 (2026-02-20)** Fixed Render deployment build. Removed `emergentintegrations==0.2.0` (internal-only, not on PyPI) and the over-constrained `fastapi==0.110.1` + `starlette<0.37` pinning. New audited `backend/requirements.txt` uses `fastapi==0.115.6`, `pydantic==2.10.4`, `motor==3.6.0`, `pymongo==4.9.2`, `bcrypt==4.2.1`, `PyJWT==2.10.1`, `pymupdf==1.27.2.3`, `python-dotenv==1.0.1`; `starlette` resolves transitively. Python pinned to **3.12.7** via `runtime.txt` (at repo root, not in `backend/`), `.python-version`, AND `PYTHON_VERSION` env in `render.yaml` (three independent signals so Render cannot fall back to 3.14). Added `/app/render.yaml` Blueprint with build/start commands and healthcheck path. All deps verified to ship cp312/abi3/py3 wheels → zero source compilation on Render.
- **v1 (2026-01-16)** PyMuPDF-based, deterministic template generator (`/app/work/make_editable.py`) producing an AcroForm-fillable PDF. Wired widgets to the embedded Roboto-Bold via AcroForm `/DR`.
- **v12 (2026-01-20)** Server-side security hardening: real auth (bcrypt + PyJWT in HttpOnly+SameSite=Lax cookie), CSRF double-submit, brute-force lockout (5 fails → 15min 429), every `/api` endpoint gated by `Depends(require_auth)`, strict input sanitisation against XSS/HTML/NoSQL/PDF injection, Helmet-equivalent security headers middleware (CSP, HSTS, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy, Permissions-Policy), `autoComplete="off"` on all forms, secure cookies auto-enabled when HTTPS. New env vars: `JWT_SECRET`, `HR_USERNAME`, `HR_PASSWORD`, `COOKIE_SECURE`. Verified: unauth → 401, wrong CSRF → 403, brute-force lockout → 429, full UI E2E (login → generate → download → logout) passes.
- **v11 (2026-01-20)** Added **document generation history**. Every certificate / offer letter / acknowledgement HR generates is now persisted in MongoDB (collection `history`) along with its rendered PDF bytes, recipient name, summary fields, file size and timestamp. A new **History** tab in the navigation lists them newest-first with:
   - Filter pills (All / Certificates / Offer Letters / Acknowledgements)
   - Name search box
   - One-click re-download (`GET /api/history/{id}/download`)
   - Delete button (`DELETE /api/history/{id}`)
  Backend endpoints: `GET /api/history?type=&q=&limit=`, `GET /api/history/{id}/download`, `DELETE /api/history/{id}`. PDFs are stored as BSON Binary (typical ~500–850 KB each, well within Mongo's 16 MB doc limit).
- **v10 (2026-01-20)** Fixed Acknowledgement letter: when HR enters an ordinal like `10th`, `12th`, `1st`, `2nd`, `3rd`, the "th"/"st"/"nd"/"rd" suffix is now auto-rendered as a **superscript** (5.83pt, baseline raised by 3pt) — matching the source PDF's `10ᵗʰ` styling exactly. Non-ordinal inputs (e.g. `Bachelor of Engineering`, `Diploma`) continue to render plain. Implemented via `_split_ordinal()` in `acknowledgement.py` plus a new `"sup"` font key wired through both the left and justified renderers.
- **v9 (2026-01-20)** Fixed font glyph mismatch reported on the Offer Letter (e.g. lowercase `a` had subtly different top hook). Rebuilt the Arial fonts using a **graft strategy**: the original ArialMT / Arial-BoldMT subsets are extracted from the source PDF and used as the BASE font (so their glyph outlines AND hinting tables — fpgm, prep, cvt, head, hhea, OS/2 — are preserved verbatim); Liberation Sans glyphs are only added for characters the source subset doesn't contain (V, j, @, x, z, q, ₹, etc.). For every character that exists in the source subset, the rendered pixels are now **byte-identical** to the original Arial — verified at 400 DPI with `0/1,483,630` differing pixels across the salary line text.
- **v8 (2026-01-20)** Added a **third template — Letter of Acknowledgement** as a separate tab. Editable fields: **Date**, **Name** (reflects in 2 places: address block + "Dear …,"), **Mark Sheet** (free text — HR can pick "10th", "12th", "Diploma", "Bachelor's Degree", "Master's Degree" or type any custom value). Implementation: `backend/acknowledgement.py` + `POST /api/ack/generate`, identical redact+re-render pipeline to the other two templates. Original header / footer / signature block / confidentiality notice are untouched. Same "type → download" UX, with a live preview on the right and a dropdown suggestion list on the Mark Sheet input.
- **v7 (2026-01-20)** Added a **login gate** at `/` with username/password = `admin / pass123` (hard-coded, client-side check appropriate for this single-tenant internal HR tool). UI matches the reference: light beige background, BluBridge logo, "Welcome back" card with icon-prefixed inputs, password visibility toggle, Forgot password link (shows contact-admin notice), dark navy pill Sign In button, footer copyright. Authentication state persists across page refreshes via `localStorage["bb_auth"]`. App header now exposes a Sign-out button that clears the flag and returns the user to the login screen.
- **v6 (2026-01-17)** Fixed font-identification issue on the Offer Letter reported via Illustrator: editable text showed as "Arimo* Bold*" (asterisks = font unavailable) while original showed "Arial Regular". Renamed Arimo's internal TTF name table to identify as "ArialMT" / "Arial-BoldMT" (same identifiers the original PDF uses). Now Adobe Illustrator and any PDF font panel report "Arial Regular" / "Arial Bold" for all text in the document — no asterisks, no font substitution. Glyph data is still Arimo (Arial-metric, Apache 2.0) so the visual appearance is identical to actual Arial.
- **v5 (2026-01-17)** Fixed two alignment issues reported on the Offer Letter:
   1. **Double ₹ symbol** — when HR typed the salary with the rupee prefix already (`₹23000/-`), the backend was adding another one. Backend now strips any leading currency prefix (`₹`, `Rs.`, `Rs`, `INR`) and trailing `/-` from the user input before re-emitting `₹<amount>/-`.
   2. **Right margin mis-alignment** — the original body paragraphs are *justified* to x=547pt, but our replaced lines were rendering left-aligned, ending short of the right margin. Added a manual word-spacing justifier (`_render_justified`) that tokenises segments into word/space tokens and distributes the extra width evenly across word gaps. The three INTERIOR justified lines (salary, commencement, annexure stipend) now end at x=546.99 — matching the original 547pt edge to within 0.01pt. Last lines of paragraphs (designation, address block, Dear, headers) remain left-aligned as in the original.
- **v4 (2026-01-17)** Added a **second template — Internship Offer Letter** as a separate menu entry. Same "type → download" UX. Editable fields:
   - Ref Code (appears once, after `CHN/2026/INT/1-`)
   - Date (reflects in 2 places: header `Date:` AND `commence on <date>` sentence)
   - Name (reflects in 2 places: address block bold heading AND `Dear` greeting)
   - Address line 1, 2, 3
   - Phone, Email
   - Designation
   - Salary Amount (reflects in 2 places: body paragraph `₹X/-` AND annexure clause 1)
   - Salary in Words (body paragraph)
  Implementation: `backend/offer_letter.py` is a config-driven per-line redact+re-render pipeline. Each editable line is white-out then redrawn segment-by-segment using bundled Arimo Regular/Bold (Arial-metric-equivalent, Apache 2.0). Position, font, size (11pt), weight, color (#000) are preserved exactly. Backend endpoint: `POST /api/offer/generate`. Frontend: `OfferLetterView` with a top-tab menu in `App.js` switching between the two templates. PDF download verified end-to-end through the public URL: 7 pages preserved, all entered values present, annexure pages and signature block intact.

## Verification (v2)
- `POST /api/template/generate` returns `application/pdf` 200 OK with proper `Content-Disposition: attachment`
- Downloaded PDF: 0 form fields, all 4 values present in content stream, original Aravind values removed, all surrounding wording / signature / footer intact
- Visual: values render in Roboto-Bold 10pt navy color, same line, same baseline as original — indistinguishable from an originally-issued certificate

## Backlog / Future
- Optional: web-based fill-in form (HR types values in the browser, server
  returns a per-employee filled PDF) — currently HR fills inside their PDF
  reader (Acrobat / Foxit / Preview / browser PDF viewer)
- Optional: bulk generation from a CSV / Google Sheet of interns
- Optional: e-signature integration so the Director need not sign each one
